# Shared / App runtime variables

# Used by artifacts/boxing-game and artifacts/api-server
PORT=24201

# Used by artifacts/boxing-game (Vite base URL path)
BASE_PATH=/

# Used by artifacts/api-server and lib/db
DATABASE_URL=postgres://user:password@localhost:5432/topboxer

# Optional runtime mode
NODE_ENV=development

# Optional Replit-specific variable for dev plugins
REPL_ID=
